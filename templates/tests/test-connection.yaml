apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demowebapplication.fullname" . }}-test-connection"
  labels:
    {{- include "demowebapplication.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demowebapplication.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
